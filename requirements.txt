# Root requirements delegate to the backend set so the two never drift.
# The backend is the source of truth (see render.yaml: rootDir: backend).
# Install from the repo root with:  pip install -r requirements.txt
-r backend/requirements.txt
